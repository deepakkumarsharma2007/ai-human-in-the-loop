[project]
name = "ai-human-in-the-loop"
version = "0.1.0"
description = "HITL Example"
readme = "README.md"
requires-python = ">=3.14"
[project]
dynamic = ["dependencies"]

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}


